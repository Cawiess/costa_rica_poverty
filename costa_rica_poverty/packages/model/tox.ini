[tox]

envlist = model
skipsdist = true

[testenv]
install_command = pip install --pre {opts} {packages}
whitelist_externals = python
deps = 
    -rrequirements.txt

setenv = 
    PYTHONPATH = .
    PYTHONHASHSEED=42

commands = 
    python model/main.py
